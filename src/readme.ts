// istanbul ignore file

/* eslint-disable no-console */

import { fakeHr } from '.';

console.log(fakeHr.competencies.all[0]);

console.log(fakeHr.education.institutions.all[0]);

console.log(fakeHr.education.qualifications.all[0]);
